# Runtime artifacts
logs/
*.log

# Dependencies
node_modules/

# Environment files
.env
.env.*

# Editor / OS scratch
.DS_Store
*.swp
*~

# test-features.mjs takes its suite lock at ./scratchpad/.suite.lock (#416). It exists only while a
# suite is running -- which is exactly when a `git add -A` from an agent will sweep it up.
scratchpad/
